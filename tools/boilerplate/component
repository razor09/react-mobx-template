import { observer } from 'mobx-react-lite'
import style from './style.scss'
import { nameProps } from './typings'

export const name = observer<nameProps>(() => {
  return (
    <div className={style.container}>name</div>
  )
})
